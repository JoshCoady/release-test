name: 'Slack Markdown Converter'
description: 'Converts Markdown to Slack mrkdwn'
inputs:
  text:
    description: 'Markdown text to convert'
    required: true
outputs:
  text:
    description: 'Converted Slack mrkdwn text'
runs:
  using: 'node12'
  main: 'index.js'
